{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <h1>Edit User</h1>

    <!-- Formulaire de mise à jour avec un bouton "Update" -->
    {{ include('cuser/_form.html.twig', {'button_label': 'Update'}) }}

    <!-- Bouton pour revenir à la liste des utilisateurs -->
    <a href="{{ path('app_cuser_index') }}" class="btn btn-secondary">Back to list</a>

    <!-- Formulaire de suppression avec un bouton de suppression -->
    {{ include('cuser/_delete_form.html.twig') }}
{% endblock %}
